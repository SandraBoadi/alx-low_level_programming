#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: alphabets in lower case
 *
 * Return:alwats 0 (Success)
 *
 */
int main(void)
{
	char omg;
		for (omg = 'a'; omg <= 'z'; omg++)
		{
			putchar(omg);
		}
		putchar('\n');
		return (0);
}

