#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Assign randon numbers
 *
 * Return: 0 if successful
 */

int main(void)
{
	int n;
	int lastDigit;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	lastDigit n = % 10
	return (0);
}
